{# @var product \inklabs\kommerce\EntityDTO\ProductDTO #}

{% set navigation = [
    {
        'name': 'Edit',
        'url' : routeUrl('admin.product.edit', {'productId': product.id.getHex()})
    },
    {
        'name': 'Images',
        'url' : routeUrl('admin.product.images', {'productId': product.id.getHex()})
    }
] %}

<div class="row">
    <div class="col-sm-12">
        {% include 'helper/tab-navigation.twig' with {'navigation': navigation} %}
    </div>
</div>

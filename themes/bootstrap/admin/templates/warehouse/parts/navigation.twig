{# @var warehouse \inklabs\kommerce\EntityDTO\WarehouseDTO #}

{% set navigation = [
    {
        'name': 'Edit',
        'url' : routeUrl('admin.warehouse.edit', {'warehouseId': warehouse.id.hex})
    },
    {
        'name': 'Inventory Locations <span class="text-muted">(' ~ warehouse.inventoryLocations|length ~ ')</span>',
        'url' : routeUrl('admin.warehouse.inventory-locations', {'warehouseId': warehouse.id.hex})
    }
] %}

<div class="row">
    <div class="col-sm-12">
        {% include '@admin/helper/tab-navigation.twig' with {'navigation': navigation} %}
    </div>
</div>

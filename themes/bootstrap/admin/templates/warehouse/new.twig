{# @var warehouse \inklabs\kommerce\EntityDTO\WarehouseDTO #}
{# @var promotionTypes array #}
{% extends '@admin/layout/base.twig' %}

{% block pageTitle %}New Warehouse{% endblock %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-sm-12">
                <h3>New Warehouse</h3>
                <form action="{{ routeUrl('admin.warehouse.new.post') }}"
                      method="post"
                      class="form-horizontal"
                      role="form">
                    <fieldset>
                        {{ form.csrf() }}
                        <div class="well">
                            {% include '@admin/warehouse/parts/warehouse-input-fields.twig' %}
                        </div>

                        <div class="well">
                            <button type="submit" class="btn btn-primary">
                                <i class="fa fa-check-circle"></i> Create
                            </button>
                            <a href="{{ routeUrl('admin.warehouse') }}" class="btn btn-default">Cancel</a>
                        </div>
                    </fieldset>
                </form>
            </div>
        </div>
    </div>
{% endblock %}

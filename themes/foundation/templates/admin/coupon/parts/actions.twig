{# @var coupon \inklabs\kommerce\EntityDTO\CouponDTO #}

<div class="row">
    <div class="col-sm-6">
        Coupon - {{ coupon.name }}
        <br>
        <br>
    </div>

    <div class="col-sm-6">
        <div class="well well-sm">
            <div>
                <form action="{{ routeUrl('admin.coupon.delete') }}"
                      method="post"
                      role="form">
                    {{ form.csrf() }}
                    {{ form.hidden('couponId', coupon.id.getHex()) }}

                    <button type="submit" class="button alert">
                        <i class="fa fa-trash"></i>
                        Delete Coupon
                    </button>
                </form>
            </div>

            <div class="clearfix"></div>
        </div>
    </div>
</div>

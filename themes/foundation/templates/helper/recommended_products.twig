{% if products is not empty %}
    <h2>{% block title %}You Might Also Like{% endblock %}</h2>
    {% if size == 'small' %}
        {% include '@theme/helper/product-grid-small.twig' with {'products': products } only %}
    {% else %}
        {% include '@theme/helper/product-grid.twig' with {'products': products } only %}
    {% endif %}
{% endif %}

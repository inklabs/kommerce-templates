{% for product in products %}
    <div>
        <h3>{{ product.name }}</h3>

        {% if product.defaultImage is empty %}
            <img src="//placehold.it/100x100&text=FallbackImg">
        {% else %}
            <img src="{{ product.defaultImage }}">
        {% endif %}

        <a href="{{ productUrl(product) }}">{{ productUrl(product) }}</a>

        {% embed "helper/rating-stars.twig" with {'rating': product.rating } only %}{% endembed %}

        {% if product.isInStock %}
            <div class="availability">
                <span class="out-of-stock">Out of Stock</span>
            </div>
        {% elseif product.isPriceVisible %}
            {% embed "helper/price.twig" with {'price': product.price } only %}{% endembed %}
        {% endif %}

        <div style="clear: both;"></div>
    </div>
{% endfor %}

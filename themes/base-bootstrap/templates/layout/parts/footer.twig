<div id="footer">
    <div class="container">
        <div class="row">
            <div class="col-sm-12">
                <a href="{{ routeUrl('page.conditions') }}">Conditions of Use</a>
                | <a href="{{ routeUrl('page.privacy') }}">Privacy</a>
                {# TODO: <?=SITE_COPY?> #}
            </div>
        </div>
    </div>
    {# TODO:
        <?php if (Kommerce::isProfilingEnabled()) { ?>
        <?=View::factory('profiler')?>
        <?php } ?>
    #}
</div>
